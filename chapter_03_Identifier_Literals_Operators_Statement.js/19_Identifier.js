// let class = "Hello World";
// console.log(class);
//class is a reserved keyword in JavaScript, so it can not be used as an Identifier. This will result in a syntax error

//let if = true;
//if is a reserved keyword in JavaScript, so it can not be used as an Identifier. This will result in a syntax error

// let return = 5;
//return is also a reserved keyword in JavaScript, so it can not be used as an Identifier. This will result in a syntax error

let myVariable = 10;
//This is valid identifier and can be used without any issue.
console.log(myVariable);

// they look like keywords but aren't reserved
let undefined = 5;//works but never do this !
let infinity = 7;//works but never do this !


